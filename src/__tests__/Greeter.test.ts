import { MyClass } from '../index';
var myclass = new MyClass();

test('My Greeter', () => {
  //expect(MyClass('Carl')).toBe('Hello Carl');
  //expect(myclass.paserhtml(`<!DOCTYPE html><p>Hello world</p>`)).toBeDefined()
});
